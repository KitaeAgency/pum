{% extends "PumWoodworkBundle::layout.html.twig" %}

{% set menu_active = "users" %}

{% block content %}
    <div class="pum-row-head">
        {{ pum_macros.section_title('Users', null, null, [
            {
                href: path('ww_homepage'),
                text: 'Woodwork'
            },
            {
                href: path('ww_user_list'),
                text: 'Users'
            },
            {
                text: 'Manage application users'
            }
        ]) }}
    </div>
    <div class="row">
        <div class="col-lg-12 col-offset-auto">
            <p>This feature is disabled because <strong>application is not using PUM user manager</strong> feature. If you want to restore feature, <a href="#">do something</a>.</p>
        </div>
    </div>
{% endblock %}
