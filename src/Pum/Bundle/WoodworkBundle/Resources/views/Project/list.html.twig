{% extends "PumWoodworkBundle::layout.html.twig" %}

{% block content %}
	<h2>
        <div class="btn-group pull-right">
            <a class="btn" href="{{ path('ww_homepage') }}"><i class="icon-chevron-left"></i>Back to homepage</a>
            <a class="btn btn-success" href="#"><i class="icon-plus icon-white"></i> Create</a>
        </div>
        Projects
    </h2>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Name</th>
                <th>Beams</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for project in projects %}
                <tr>
                    <td>
                        {{ project.name }}
                    </td>
                    <td>
                        {% for beam in project.beams %}
                            {{ beam.name }}<br />
                        {% endfor %}
                    <td>
                        <a class="btn" href="#"><i class="icon-eye-open"></i> View</a>
                </tr>
            {% else %}
                <tr class="info">
                    <td colspan="2"><em>No beam found. <a href="#">Create a new beam</a></td>
                </em>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
