{# ARRRAY #}
{% if value is empty %}
    <span class="val-empty">{{ "common.generic.empty"|trans({}, 'pum') }}</span>
{% else %}
    {% for v in value %}
        <div>{{ v }}</div>
    {% endfor %}
{% endif %}
