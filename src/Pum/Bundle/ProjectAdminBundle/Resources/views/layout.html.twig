{% extends "PumAppBundle::layout.html.twig" %}

{% set menu_active  = null %}
{% set app_active  = 'projectadmin' %}

{% set pum_menu = [] %}

{% for beam in pum_project().beams %}
    {% set pum_menu = pum_menu|merge({(beam.name): {
        'color': beam.color,
        'icon':  beam.icon,
        'title': beam.name|humanize,
        'path': 'ww_homepage'
    }}) %}
{% endfor %}
