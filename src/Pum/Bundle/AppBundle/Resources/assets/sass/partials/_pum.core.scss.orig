/* Body
-------------------------------------------------- */
html, body { height: 100%; }
html {
    font: $typo-default-font;
}
body {
    background-color: $main-background-color;
    color: $main-text-color;
    -webkit-font-smoothing: antialiased;
}
a, a strong {
    @include transition(all .15s ease-in);
}

.pull-right + .pull-right {
    margin-right: 1%;
}

/* Bootstrap changes
-------------------------------------------------- */
label.list-group-item:hover, label.list-group-item:focus {
  @extend a.list-group-item:hover, a.list-group-item:focus;
}

<<<<<<< HEAD
/* Helpers
=======
.tooltip {
    -webkit-font-smoothing: subpixel-antialiased;
}

/* Typo
-------------------------------------------------- */
h1, h2, h3, h4, h5, h6, .h1, .h2, .h3, .h4, .h5, .h6 {
    font-family: $typo-default-fontfamily;
}
h1, h2, h3 { margin: 0; }
.pum-view_title { line-height: 1.3; font-size: 2em; font-weight: normal; }

h1 small, .h1 small { padding-left: 5px; font-size: 55%; color: $text-color-gray-muted;}

.text-muted {
    color: $text-color-gray-muted;
}

code.text-muted {
    color: #BB7889;
    font-size: 80%;
}

.text-baseline {
    margin: 10px 0 0 0;
    font-size: 70%;
}

/* Container
-------------------------------------------------- */
.pum-core-container {
    padding-top: 60px;
    height: 100%;
}
.pum-core-height {
    position: relative;
    min-height: 100%;
}

/* Content
-------------------------------------------------- */
.pum-core-content {
    @include calc(max-width, 100% - #{$layout-sidebar-width});
    display: inline-block;
    margin-left: $layout-sidebar-width;
    padding: 15px 20px;
    width: 100%;
    height: 100%;

    & .col-lg-12 {
        margin: auto;
        max-width: 1100px;
    }
}

    .pum-row-head {
        position: relative;
        margin: -15px -20px 15px;
        padding: 11px 20px;
        border-bottom: 1px solid darken($content-head-background-color, 2.5%);
        background-color: $content-head-background-color;
        cursor: default;

        & .wbcrmb {
            @include transition(all .15s ease-in);
        }

        & .breadcrumb {
            @include transition(all .15s ease-in);
            position: absolute;
            bottom: 0;
            left: 0;
            z-index: 2;
            margin: 0;
            padding: 0 10px 12px 20px;
            border-radius: 0;
            border-radius: 0 4px 0 0;
            background-color: inherit;
            color: inherit;
            font-size: 85%;
            opacity: 0;
        }

        &:hover {
            & .wbcrmb {
                opacity: 0;
            }

            & .breadcrumb {
                opacity: 1;

                & > .active {
                    color: $text-color-gray-muted;
                }
            }
        }

        & .pum-action-bar {
            margin-top: 10px;
        }
    }
    .table + .pum-action-bar {
        margin-top: -20px;
        padding: 0 20px;

        & .btn-small {
            margin-top: 3px;
            padding-top: 2px;
            padding-bottom: 2px;
            font-size: 12px;
        }
    }

    .pum-core-content hr {
        border-color: #DDD;
    }

    .thumbnail:hover {
        text-decoration: none;
    }

    .val-empty {
        opacity: 0.55;
        font-size: 90%;

        &:before {
            content: "< ";
            opacity: 0.4;
        }
        &:after {
            content: " >";
            opacity: 0.4;
        }
    }

/* Titles
>>>>>>> master
-------------------------------------------------- */
.vhidden {
    visibility: hidden;
}
