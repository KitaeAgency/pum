Pum\Core\Definition\FormView:
    type: entity
    table: schema_object_form_view
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    fields:
        name:
            type: string
            length: 100
        private:
            column: is_private # private is a reserved keyword
            type: boolean
        rows:
            type: json_array
    manyToOne:
        objectDefinition:
            targetEntity: ObjectDefinition
            inversedBy: formViews
            onDelete: CASCADE
            joinColumn:
                name: schema_object_id
                referencedColumnName: id
    uniqueConstraints:
        form_view_name_index:
            columns: schema_object_id, name
